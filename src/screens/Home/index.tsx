// Archivo principal que exporta el componente de Home
// Sirve como punto de entrada para mantener compatibilidad con importaciones existentes

import HomeView from './view/HomeView';

// Exportamos el componente de vista como componente principal de Home
export default HomeView;