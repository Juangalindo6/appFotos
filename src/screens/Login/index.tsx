// Archivo principal que exporta el componente de Login
// Sirve como punto de entrada para mantener compatibilidad con importaciones existentes

import LoginView from './view/LoginView';

// Exportamos el componente de vista como componente principal de Login
export default LoginView;